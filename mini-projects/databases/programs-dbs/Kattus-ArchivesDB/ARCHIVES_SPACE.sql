CREATE TABLE book (
    isbn    VARCHAR2(13) PRIMARY KEY NOT NULL,
    title   VARCHAR2(50) NOT NULL,
    author  VARCHAR2(50) NOT NULL,
    genre   VARCHAR2(50),
    CONSTRAINT chk_ibsn CHECK (isbn BETWEEN 0000000000000 AND 9999999999999)
);

DROP TABLE book;